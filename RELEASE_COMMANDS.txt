python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python scripts\bump_version.py patch
pyinstaller --clean SAP-Role-Updater.spec
$version = python -c "from version import APP_VERSION; print(APP_VERSION)"
git add .
git commit -m "Prepare release v$version"
git push origin master
git tag "v$version"
git push origin "v$version"
gh release create "v$version" "dist/SAP Role Updater.exe#SAP Role Updater.exe" "templates/RULES_template.csv#RULES_template.csv" -t "SAP Role Updater v$version" -F RELEASE_NOTES.md
